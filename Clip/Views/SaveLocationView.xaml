<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Clip.Views.SaveLocationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource ClipCardStyle}">
        <StackPanel Spacing="12">
            <StackPanel Spacing="3">
                <TextBlock Text="{Binding Text.SaveLocationTitle}" Style="{StaticResource SectionTitleTextStyle}" />
                <TextBlock Text="{Binding Text.SaveLocationDescription}" Style="{StaticResource CaptionTextStyle}" />
            </StackPanel>
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    MinHeight="44"
                    Text="{Binding DefaultSaveFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" Click="OnBrowseClick" ToolTipService.ToolTip="{Binding Text.ChooseFolderTooltip}">
                    <FontIcon Glyph="&#xE8B7;" FontSize="15" />
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
